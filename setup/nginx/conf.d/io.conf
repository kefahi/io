upstream io-fpm {
  server unix:/home/kefah/Development/run/php-fpm.sock;
}

server {
  listen       80 default_server;
  listen       [::]:80 default_server;
  server_name  _;
  root         /home/kefah/Development/public;

  server_tokens off;

  index index.php;
  #try_files $uri $uri/ index.php /index.php;
  access_log /home/kefah/Development/logs/nginx-access.log;
  error_log  /home/kefah/Development/logs/nginx-error.log;
  fastcgi_read_timeout  600;
  fastcgi_max_temp_file_size 0;

  gzip             on;
  gzip_vary        on;
  gzip_types       text/javascript text/css text/xml application/xml application/xml+rss;
  gzip_comp_level  9;
  gzip_min_length  100;
  gzip_buffers 16 8k;
  gzip_proxied     expired no-cache no-store private auth;

  location ~ \.(js|css|png|jpg|gif|swf|ico|pdf|mov|fla|zip|rar)$ {
    try_files $uri =404;
    access_log off;
  }

  #error_page 404 /404.html;
  #error_page 500 502 503 504 /50x.html;

  location ~ \.php$ {
    try_files $uri =404;
    fastcgi_split_path_info ^(.+\.php)(/.+)$;
    include fastcgi_params;
    fastcgi_param  PATH_INFO        $fastcgi_path_info;
    fastcgi_index index.php;
    fastcgi_param  SCRIPT_FILENAME  $document_root$fastcgi_script_name;
    fastcgi_pass io-fpm;
  }
  location /redisman {
    try_files $uri $uri/ /redisman/web/index.php?$args;
  }

  location ~ ^/(fpm-status|fpm-ping)$ {
    include fastcgi_params;
    fastcgi_param PATH_INFO        $fastcgi_path_info;
    fastcgi_param  SCRIPT_FILENAME  $document_root$fastcgi_script_name;
    access_log off;
    fastcgi_pass io-fpm;
  }

  location /ncheck {
    access_log off;
    stub_status on;
  }

  location ~ /\.(ht|svn|git) {
    access_log off;
    deny all;
  }
}

